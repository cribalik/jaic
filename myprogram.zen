
var v: []int
// some variables
var var1 = (3+5)+5
var var2 = var1
var var3: int = 3
var var4: float = 3.01+5.0
var var5 = fun2(var4, var1)
var myvec2: vec2
var mycomp : compound
var var6 = fun3(myvec2, mycomp)
var var7: vec2 = {x: 3, y: 5}
var var8: vec3 = {x: 3, y: 5}
var var9: compound = {a: {x: 3, y: 5}}
var var10 = overfun(3, 3.0)
// var7 := vec2{x = 3, y = 5}

// some functions
fn fun2(a:float, b:int): int {} // should give error
fn fun3(a:vec2, b:compound): vec2 { fun2(0.5, a.x); }
fn fun4() {}
fn add(a: int, b: int): int {
  return a + b
}

// some foreign (C) functions
fn putchar(a:int): int #foreign
fn rand(): int #foreign
fn printf(fmt: ^i8, ...): int #foreign


// function overloading is OK
var result = overfun(1, 1.0);
fn overfun(a: vec2) {};
fn overfun(b: vec3) {};
fn overfun(a: int, b: float) {};

var some_identifier = 3;
// type some_identifier {a:int} // identifiers must be unique, this should error

// some structs
type compound {
  a: vec2
  b: vec3
}

type compound2 {
  a:int
  b:int
}

type vec3 {
  x: int
  y: int
  z: int
}

type vec2 {
  x: int
  y: int
}

// some nested structs
type B {
  v: vec2
  // a: A // will cause error because A will contain itself
}

type A {
  b: B
}

type Weapon {
  damage: int
  durability: int
}

type Monster {
  gold: int
  health: int
  weapon: Weapon
}

fn newRichMonster(): Monster {
  var some_gold = 300
  var more_gold = (2 * 2300) + 300
  return {gold: some_gold + more_gold*2}
}

// should not compile
/*
fn should_not_compile() : int {
  // variable not declared!
  putchar(a)
  a := 3

  // recursive type
  /*
  type A {
    b: B
  }
  type B {
    a: A
  }
  */

  // no return value!
}
*/

fn main() {

  // this should be allowed for r-value but not for l-value
  // newRichMonster().gold = 3

  var m: Monster = {gold: 3}
  putchar(56 + m.gold)

  var str: [5]i8
  str[0] = 97
  str[1] = 98
  str[2] = 99
  str[3] = 100
  str[4] = 0

  printf("hello %s %i %f", "hej", 7, 0.13)

  var arr: [2][2]int
  arr[0][1] = 1
  arr[1][0] = 2
  arr[1][1] = 3

  var dynarr: []int = arr[1]

  for a,i : arr {
    printf("%i\n", i);
    for b : a {
      printf("%i\n", b)
    }
  }

  for a,i : dynarr {
    printf("Value is %i\n", a)
  }

}
